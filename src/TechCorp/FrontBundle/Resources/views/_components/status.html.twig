<div class="row status-container">
	<div class="col-md-12">
		<p>Par <a href="{{ path('tech_corp_front_user_timeline', { userId : status.user.id }) }}">{{ status.user.username }}</a></p>
		<p>{{ status.content }}</p>
		<p>{{ status.createdAt|date('Y-m-d H:i:s') }}</p>
	</div>
</div> 